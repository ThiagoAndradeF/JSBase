estaEntre=function(numero,minimo,maximo,inclusivo=false){
    let entre
    if(minimo<numero>maximo){
        entre===true
    }
    if (inclusivo==true){ 
        if(minimo<=numero>=maximo){
        entre===inclusivo
    }}
    if (inclusivo==false){ 
        if(minimo<=numero>=maximo){

    }}
}